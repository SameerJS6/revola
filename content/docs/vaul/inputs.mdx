---
title: Inputs
description: All the Inputs Examples ported to Revola from Vaul.
---

Traditionally tricky to get right, because keyboard is pushing the content up. Vaul fixes this by correcting the position of the drawer when the keyboard is opened.

<ComponentPreview name="default-input-demo" />

### No repositioning
Setting `repositionInputs` to false to disable the repositioning of inputs and fallback to the default browser behavior.

<ComponentPreview name="no-repositioning-input-demo" />
